server {
    listen 80;
    server_name flowsctrl.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 "OK HTTP (temporal) para certbot\n";
        add_header Content-Type text/plain;
    }
}
